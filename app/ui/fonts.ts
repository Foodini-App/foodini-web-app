import { Inter } from "next/font/google";
import { Heebo } from "next/font/google";

export const heeboRegular = Heebo({ subsets: ["latin"], weight: "400" });
export const heeboBold = Heebo({ subsets: ["latin"], weight: "900" });
export const inter = Inter({ subsets: ["latin"] });
